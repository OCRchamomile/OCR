либо через комадную строку, либо через терминал vsc

pip install -r requirements.txt - загрузка необходимых библиотек
запуск сайта:
сначала перейдите в папку в командной строке, с помощью cd, потом введите python -m uvicorn mainapp:app